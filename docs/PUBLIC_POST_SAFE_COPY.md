# Public Post Safe Copy

Use this version until the exact `0.96 -> 0.008` incident is linked to one auditable receipt bundle.

```md
"It scored 96%." Cool -- does it actually work?

I watched a coding-agent loop drive a benchmark number up while its real reusable capability stayed
far lower. In NodeRoom, selected/family-writer BankerToolBench runs reached about 0.96, but the
clean-probe lane told a much more uncomfortable story: once task-specific materializers, replay
paths, and self-certified clean labels were quarantined, the number had to be reported with caveats,
split state, writer state, and live-UI proof.

That gap is exactly what breaks demos when they meet real users.

So at The Super Solo: Agent Skills Hack Day, co-hosted with Inference.ai, I shipped:

Solo Founder Agent Builder + Eval Loop Engineering Skill Nodes

Benchmark-driven development for AI agents.

Point Claude Code, Codex, or any coding agent that can read markdown and run shell commands at it,
and it gives the agent one honest loop to run on your app:

discover -> pick a real benchmark -> set it up -> build the agent + UI -> wire the adapter ->
iterate -> verify in your live UI.

The core rule is simple: no answer keys, no self-graded claims, no benchmark number without receipts,
and no "works in product" claim unless the live app UI proves it.

Install it, run the conformance probe, and tell me what it caught.
```

## Do Not Post Yet

Do not use the sentence below until the exact public receipt bundle exists:

```md
I watched a coding-agent fleet push a benchmark to 0.96 while the agent's real, held-out capability
was 0.008.
```

The current repo has adjacent receipts for high selected/family-writer scores, a much lower
pre-materializer/general signal, and a separate `0.008333` SpreadsheetBench row. That is enough to
explain the failure pattern. It is not enough to publish the exact sentence as one incident.
